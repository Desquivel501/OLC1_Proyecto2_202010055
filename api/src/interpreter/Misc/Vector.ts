import { Type } from "../Expresion/Retorno";

export class Vector1 {
    constructor(public valor:any, public id:string, public largo, public type:Type ){ }
}

export class Vector2 {
    constructor(public valor:any, public id:string, public largo_i, public largo_j, public type:Type ){ }
}